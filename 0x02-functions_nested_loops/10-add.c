#include <unistd.h>
#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 * @a: right operand
 * @b: left operand
 *
 * Return: the result
 */
int add(int a, int b)
{
	return (a + b);
}
